# Allure Configuration
allure.tests.management.pattern=https://[PROJECT_ID]/browse/{0}
allure.issue.tracker.pattern=https://[PROJECT_ID]/issues/{0}

# Report configuration
allure.report.secure=true

# Test name template
allure.resultsFormat=json

# Categories configuration
allure.issue.tracker.plugin=junit-xml
